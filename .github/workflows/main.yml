name: CI 

on: 
  push:
    branches: ["main]
  pull_request:
    branches: [main]

    workflow_dispatch:

  jobs:

  build:
  runs on: ubuntu-latest

  steps: 
  -uses: actions/checkout@v3

  -name: install node package
  -run: npm install

  -name: Build output.css
  -run: npm run build
